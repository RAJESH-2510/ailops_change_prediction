{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Main Flow",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/flows/main_flow.py",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
